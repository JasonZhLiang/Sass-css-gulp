{
  "name": "Sass-css",
  "version": "1.0.0",
  "scripts": {
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp watch",
    "start": "npm run gulp && concurrently \"npm run gulp-watch\" \"npm run lite\""
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.2",
    "lite-server": "^2.4.0"
  }
}
